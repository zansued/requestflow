```json
{
  "wave": 2,
  "chain": ["code_architect", "developer", "integration_agent"],
  "content": "import React, { Suspense, lazy } from 'react';\nimport { \n  createBrowserRouter, \n  RouterProvider, \n  Navigate, \n  Outlet,\n  createRoutesFromElements,\n  Route,\n  useRouteError,\n  LoaderFunctionArgs,\n  redirect,\n  LoaderFunction\n} from 'react-router-dom';\nimport MainLayout from '../components/layout/MainLayout';\n\n// Tipos para os dados dos loaders\ntype RequestDetailLoaderData = {\n  id: string;\n};\n\n// Interface tipada para usuário\ninterface User {\n  id: string;\n  name: string;\n  email: string;\n  role: 'admin' | 'user' | 'auditor';\n  permissions: string[];\n}\n\ntype AuthLoaderData = {\n  isAuthenticated: boolean;\n  user: User | null;\n};\n\n// Lazy loading para otimização de performance\nconst DashboardPage = lazy(() => import('../pages/DashboardPage'));\nconst RequestsPage = lazy(() => import('../pages/RequestsPage'));\nconst RequestDetailPage = lazy(() => import('../pages/RequestDetailPage'));\nconst TemplatesPage = lazy(() => import('../pages/TemplatesPage'));\nconst AuditPage = lazy(() => import('../pages/AuditPage'));\nconst LoginForm = lazy(() => import('../components/auth/LoginForm'));\nconst RegisterForm = lazy(() => import('../components/auth/RegisterForm'));\n\n// Preload para rotas críticas (dashboard)\nconst preloadCriticalRoutes = () => {\n  const preloadDashboard = () => import('../pages/DashboardPage');\n  // Pré-carrega o dashboard após carregamento inicial\n  setTimeout(preloadDashboard, 1000);\n};\n\n// Componente de Loading reutilizável\nconst LoadingSpinner = () => (\n  <div className=\"flex items-center justify-center min-h-screen\">\n    <div className=\"animate-spin rounded-full h-12 w-12 border-b-2 border-primary\"></div>\n  </div>\n);\n\n// Componente de Error Boundary real usando class component\nclass RouteErrorBoundary extends React.Component<{\n  children: React.ReactNode;\n}, {\n  hasError: boolean;\n  error: Error | null;\n}> {\n  constructor(props: { children: React.ReactNode }) {\n    super(props);\n    this.state = { hasError: false, error: null };\n  }\n\n  static getDerivedStateFromError(error: Error) {\n    return { hasError: true, error };\n  }\n\n  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {\n    console.error('Erro capturado pelo Error Boundary:', error, errorInfo);\n  }\n\n  render() {\n    if (this.state.hasError) {\n      return (\n        <div className=\"min-h-screen flex items-center justify-center bg-gray-50\">\n          <div className=\"text-center\">\n            <h2 className=\"text-2xl font-bold text-red-600 mb-4\">Algo deu errado</h2>\n            <p className=\"text-gray-600 mb-6\">Ocorreu um erro ao carregar esta página.</p>\n            <button\n              onClick={() => window.location.reload()}\n              className=\"px-4 py-2 bg-primary text-white rounded-lg hover:bg-primary-dark transition-colors\"\n            >\n              Recarregar Página\n            </button>\n          </div>\n        </div>\n      );\n    }\n\n    return this.props.children;\n  }\n}\n\n// Componente para tratamento de erros de rota\nconst RouteErrorPage = () => {\n  const error = useRouteError() as Error;\n  \n  return (\n    <div className=\"min-h-screen flex items-center justify-center bg-gray-50\">\n      <div className=\"text-center\">\n        <h1 className=\"text-3xl font-bold text-red-600 mb-4\">\n          {error instanceof Error ? error.message : 'Erro inesperado'}\n        </h1>\n        <p className=\"text-gray-600 mb-6\">\n          Ocorreu um erro ao processar sua requisição.\n        </p>\n        <button\n          onClick={() => window.location.href = '/dashboard'}\n          className=\"px-4 py-2 bg-primary text-white rounded-lg hover:bg-primary-dark transition-colors\"\n        >\n          Voltar para o Dashboard\n        </button>\n      </div>\n    </div>\n  );\n};\n\n// Função para verificar token via API (simulação)\nconst verifyTokenWithAPI = async (token: string): Promise<{ valid: boolean; user?: User }> => {\n  try {\n    // Em produção, isso seria uma chamada real à API\n    const response = await fetch('/api/auth/verify', {\n      method: 'POST',\n      headers: {\n        'Authorization': `Bearer ${token}`,\n        'Content-Type': 'application/json',\n      },\n    });\n\n    if (response.ok) {\n      const data = await response.json();\n      return { valid: true, user: data.user };\n    }\n    return { valid: false };\n  } catch (error) {\n    console.error('Erro na verificação do token:', error);\n    return { valid: false };\n  }\n};\n\n// Função auxiliar para verificar autenticação\nconst checkAuth = async (): Promise<AuthLoaderData> => {\n  try {\n    const userData = localStorage.getItem('axionos_user');\n    const token = localStorage.getItem('axionos_token');\n    \n    if (!userData || !token) {\n      return { isAuthenticated: false, user: null };\n    }\n\n    // Verifica token via API (em produção)\n    const tokenVerification = await verifyTokenWithAPI(token);\n    \n    if (!tokenVerification.valid) {\n      // Token inválido, limpa localStorage\n      localStorage.removeItem('axionos_user');\n      localStorage.removeItem('axionos_token');\n      return { isAuthenticated: false, user: null };\n    }\n\n    // Se a API retornou dados do usuário, usa-os\n    if (tokenVerification.user) {\n      return { \n        isAuthenticated: true, \n        user: tokenVerification.user \n      };\n    }\n\n    // Fallback para dados do localStorage\n    const user = JSON.parse(userData) as User;\n    return { isAuthenticated: true, user };\n  } catch (error) {\n    console.error('Erro ao verificar autenticação:', error);\n    return { isAuthenticated: false, user: null };\n  }\n};\n\n// Loader para verificação de autenticação (apenas dados)\nconst authDataLoader: LoaderFunction = async () => {\n  const authData = await checkAuth();\n  return authData;\n};\n\n// Middleware para proteção de rotas\nconst protectedRouteMiddleware = async ({ request }: LoaderFunctionArgs) => {\n  const authData = await checkAuth();\n  const url = new URL(request.url);\n  const pathname = url.pathname;\n  \n  // Rotas públicas que não requerem autenticação\n  const publicRoutes = ['/login', '/register'];\n  const isPublicRoute = publicRoutes.includes(pathname);\n  \n  // Se o usuário está autenticado e tenta acessar rota pública, redireciona para dashboard\n  if (authData.isAuthenticated && isPublicRoute) {\n    return redirect('/dashboard');\n  }\n  \n  // Se o usuário não está autenticado e tenta acessar rota protegida, redireciona para login\n  if (!authData.isAuthenticated && !isPublicRoute) {\n    return redirect('/login');\n  }\n  \n  return null;\n};\n\n// Loader para página de detalhes da solicitação\nconst requestDetailLoader: LoaderFunction<RequestDetailLoaderData> = async ({ params }) => {\n  const { id } = params;\n  \n  // Validação do ID\n  if (!id || typeof id !== 'string' || id.trim().length === 0) {\n    throw new Response('ID da solicitação inválido', { status: 400 });\n  }\n  \n  // Validação de formato (exemplo: UUID ou número)\n  const isValidId = /^[0-9a-fA-F-]{36}$/.test(id) || /^\\d+$/.test(id);\n  if (!isValidId) {\n    throw new Response('Formato do ID inválido', { status: 400 });\n  }\n  \n  // Aqui você pode adicionar lógica para buscar os dados da solicitação\n  // Por enquanto, apenas retorna o ID validado\n  return { id };\n};\n\n// Wrapper para rotas protegidas com Suspense e Error Boundary\nconst ProtectedRouteElement = ({ element }: { element: React.ReactNode }) => (\n  <RouteErrorBoundary>\n    <Suspense fallback={<LoadingSpinner />}>\n      {element}\n    </Suspense>\n  </RouteErrorBoundary>\n);\n\n// Wrapper para rotas públicas com Suspense e Error Boundary\nconst PublicRouteElement = ({ element }: { element: React.ReactNode }) => (\n  <RouteErrorBoundary>\n    <Suspense fallback={<LoadingSpinner />}>\n      {element}\n    </Suspense>\n  </RouteErrorBoundary>\n);\n\n// Componente Layout com Outlet para rotas aninhadas\nconst LayoutWrapper = () => {\n  return (\n    <MainLayout>\n      <Outlet />\n    </MainLayout>\n  );\n};\n\n// Definição das rotas usando createRoutesFromElements\nconst routeElements = createRoutesFromElements(\n  <Route \n    path=\"/\" \n    loader={protectedRouteMiddleware}\n    errorElement={<RouteErrorPage />}\n  >\n    {/* Rotas públicas */}\n    <Route \n      path=\"login\" \n      element={<PublicRouteElement element={<LoginForm />} />} \n      loader={authDataLoader}\n    />\n    <Route \n      path=\"register\" \n      element={<PublicRouteElement element={<RegisterForm />} />} \n      loader={authDataLoader}\n    />\n    \n    {/* Rotas protegidas com layout principal */}\n    <Route \n      path=\"/\" \n      element={<LayoutWrapper />} \n      loader={authDataLoader}\n    >\n      <Route \n        index \n        element={<Navigate to=\"/dashboard\" replace />} \n      />\n      <Route \n        path=\"dashboard\" \n        element={<ProtectedRouteElement element={<DashboardPage />} />} \n      />\n      <Route \n        path=\"requests\" \n        element={<ProtectedRouteElement element={<RequestsPage />} />} \n      />\n      <Route \n        path=\"requests/:id\" \n        element={<ProtectedRouteElement element={<RequestDetailPage />} />} \n        loader={requestDetailLoader}\n      />\n      <Route \n        path=\"templates\" \n        element={<ProtectedRouteElement element={<TemplatesPage />} />} \n      />\n      <Route \n        path=\"audit\" \n        element={<ProtectedRouteElement element={<AuditPage />} />} \n      />\n      \n      {/* Rota de fallback para 404 */}\n      <Route \n        path=\"*\" \n        element={(\n          <div className=\"min-h-screen flex items-center justify-center\">\n            <div className=\"text-center\">\n              <h1 className=\"text-4xl font-bold text-gray-800 mb-4\">404</h1>\n              <p className=\"text-gray-600 mb-6\">Página não encontrada</p>\n              <button\n                onClick={() => window.location.href = '/dashboard'}\n                className=\"px-4 py-2 bg-primary text-white rounded-lg hover:bg-primary-dark transition-colors\"\n              >\n                Voltar para o Dashboard\n              </button>\n            </div>\n          </div>\n        )} \n      />\n    </Route>\n  </Route>\n);\n\n// Configuração do router com basename para deploy em subdiretórios\nconst router = createBrowserRouter(\n  routeElements,\n  {\n    basename: process.env.PUBLIC_URL || '/',\n    future: {\n      v7_normalizeFormMethod: true,\n      v7_fetcherPersist: true,\n    },\n  }\n);\n\n// Componente principal do Router\nconst AppRouter = () => {\n  React.useEffect(() => {\n    // Pré-carrega rotas críticas após renderização inicial\n    preloadCriticalRoutes();\n  }, []);\n\n  return (\n    <RouterProvider \n      router={router} \n      fallbackElement={<LoadingSpinner />}\n    />\n  );\n};\n\nexport default AppRouter;\nexport type { User, AuthLoaderData, RequestDetailLoaderData };"
}
```

## Resumo das Correções

1. **Código Completo**: Agora inclui toda a definição das rotas, configuração do router e exportações.

2. **Tipo `User` Definido**: Substituído `any` por interface tipada com propriedades específicas.

3. **Autenticação Aprimorada**:
   - Adicionada verificação de token via API (`verifyTokenWithAPI`)
   - Separação entre `authDataLoader` (dados) e `protectedRouteMiddleware` (redirecionamentos)
   - Limpeza de localStorage quando token é inválido

4. **Basename Configurado**: Adicionada configuração de `basename` no `createBrowserRouter` para suporte a deploy em subdiretórios.

5. **Pré-carregamento de Rotas Críticas**: Implementada função `preloadCriticalRoutes` para melhorar performance.

6. **Estrutura de Rotas Completa**:
   - Rotas públicas (`/login`, `/register`)
   - Rotas protegidas com layout principal
   - Rota de fallback 404
   - Redirecionamento padrão para `/dashboard`

7. **Exportação de Tipos**: Exportação dos tipos para uso em outros módulos.

8. **Configurações Future do React Router**: Adicionadas flags para comportamentos futuros.

9. **Tratamento de Erros Aprimorado**: Error Boundary e página de erro específica para rotas.

10. **Loader Separado**: Separação clara entre loaders de dados e middleware de proteção.